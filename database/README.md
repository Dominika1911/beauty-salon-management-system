# Beauty Salon – baza danych (PostgreSQL + Docker)

## 1) Przygotowanie
- Przejdź do folderu `database/`.
- Skopiuj `.env.example` → `.env` i (opcjonalnie) zmień hasło.

## 2) Start (port hosta 5433)
```bash
docker compose up -d
